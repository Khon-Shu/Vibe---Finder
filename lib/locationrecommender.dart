import 'package:flutter/material.dart';

class Locationrecommender extends StatelessWidget {
  const Locationrecommender({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}